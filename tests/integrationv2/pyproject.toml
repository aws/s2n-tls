[project]
name = "integrationv2"
version = "0.0.1"
description = "Integration tests for s2n-tls"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "pytest>=8.3.4",
    "pytest-rerunfailures>=15.0",
    "pytest-xdist>=3.6.1",
    "ruff>=0.9.7",
    "sslyze>=6.1.0",
]
