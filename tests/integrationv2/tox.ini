[tox]
envlist = py39
skipsdist = True

[testenv]
# install pytest in the virtualenv where commands will be executed
setenv = S2N_INTEG_TEST = 1
passenv = DYLD_LIBRARY_PATH, LD_LIBRARY_PATH, OQS_OPENSSL_1_1_1_INSTALL_DIR, HOME, TOX_TEST_NAME
ignore_errors=False
deps =
    pytest==7
    pytest-xdist==3.3
    sslyze==5
    pytest-rerunfailures
# -x : exit on first failure
# -n auto : launch number of workers equal to number of cores
# -rpfsq : print a (r)eport with (p)assed tests, (f)ailed tests, and (s)kipped tests
# --durations=100 : print the 100 slowest tests
commands =
    pytest -x \
        -n=2 \
        --maxfail=1 \
        --reruns=2 \
        --cache-clear \
        -rpfsq \
        --durations=100 \
        -o log_cli=true --log-cli-level=INFO \
        --provider-version={env:S2N_LIBCRYPTO} \
        --provider-criterion={env:S2N_USE_CRITERION:"off"} \
        --fips-mode={env:S2N_TEST_IN_FIPS_MODE:"0"} \
        --no-pq={env:S2N_NO_PQ:"0"} \
        {env:TOX_TEST_NAME:""}
